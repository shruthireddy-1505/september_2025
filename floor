class Solution:
    def findFloor(self, arr, x):
        # code here
        l=0
        h=len(arr)-1
        ans=-1
        while l<=h:
            mid=(l+h)//2
            if x>=arr[mid]:
                ans=mid
                l=mid+1
            else:
                h=mid-1
        return ans
