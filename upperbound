class Solution:
    def upperBound(self, arr, target):
        # code here
        l=0
        h=len(arr)-1
        while l<=h:
            mid=(l+h)//2
            if target<arr[mid]:
                h=mid-1
            else:
                l=mid+1
        return l
